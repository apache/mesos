#!/bin/bash
bin=`dirname "$0"`
bin=`cd "$bin"; pwd`

. $bin/mesos_env.sh

$bin/start-master

$bin/start-slaves

echo "Everything's started! You can view the master Web UI at"

echo "      http://$MASTER:$MASTER_WEBUI_PORT"
