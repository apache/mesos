#ifndef __CONFIG_HPP__
#define __CONFIG_HPP__

/* This header gets completed by configure. */

#include <string>

/* Constants. */
const std::string ZNODE = "/nxmaster";

#if @WITH_WEBUI@
  #define NEXUS_WEBUI
#endif

#endif /* __CONFIG_HPP__ */
